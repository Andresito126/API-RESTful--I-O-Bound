import { FileFormatStrategy } from "../interfaces/FileFormatStrategy";

export class PDFFileFormatStrategy extends FileFormatStrategy  {
    convertTo(files){
        // Lógica para convertir de un tipo de archivo a PDF    
    }
}
